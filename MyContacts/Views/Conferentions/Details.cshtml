@model MyContacts.ViewModels.ConferentionViewModel

<h1>Details</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Conferention.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Conferention.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.MembersPhoneNumbers)
        </dt>
        
        <dd class="col-sm-10">
            @foreach (var item in Model.MembersPhoneNumbers)
            {
               <div>@item</div>
            }
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Conferention.PlaningDate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Conferention.PlaningDate)
        </dd>
    </dl>
</div>
<div>
    <a asp-controller="ConferentionsMembers" asp-action="Index" asp-route-id="@Model.Conferention.Id">Manage members</a> |
    <a asp-action="Edit" asp-route-id="@Model.Conferention.Id">Edit</a> |
    <a asp-action="Delete" asp-route-id="@Model.Conferention.Id">Delete</a>
</div>
<a asp-action="Index">Back to List</a>